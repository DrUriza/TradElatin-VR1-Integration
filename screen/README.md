# TradELATIN VR1 Screen V4.2

Standalone Dash/HMI repository. It does **not** contain or start the Emulator and it does not start Processing.
The repository includes the latest eight validated contracts as initial data,
so the HMI is usable immediately after cloning; a separately running Processing
repository can replace them through `TRADELATIN_CONTRACT_DIR`.

The current public HMI represents **C1-C8 only**. These families are implemented
and validated primarily through emulator, synthetic-data, and replay workflows.
The HMI does not contact market or blockchain providers; it consumes versioned
contracts generated by Processing. Live-provider availability and effective
source status are determined by Processing and only rendered by Screen.

## Configuration

- `TRADELATIN_CONTRACT_DIR`: directory containing the eight final JSON contracts. Default: `./data/contracts`.
- `TRADELATIN_REFRESH_DIR`: directory where manual refresh requests are written. Default: `./data/refresh_requests`.
- `TRADELATIN_SOURCE_CONTROL_PATH`: shared HMI source-selection JSON.
- `TRADELATIN_SOURCE_STATUS_DIR`: shared Processing source-status directory.
- `TRADELATIN_HOST`: default `127.0.0.1`.
- `TRADELATIN_PORT`: default `8002`.

Automatic UI reread policy:

- Prices: 5 s
- Liquidity: 5 s
- CVD: 15 s

CVD timeframes: `5m`, `15m`, `4h` only. Screen rejects stale CVD contracts outside this set.

OI, ETF, On-Chain, Volatility and Liquidations use the manual Reload button.

The header source control exposes desired `LIVE` and `SYNTHETIC` modes. Its
separate badge reports the actual `LIVE`, `HYBRID`, `SYNTHETIC FALLBACK` or
`VALIDATING` state and provider details. Screen never contacts providers; it
publishes the selection and renders Processing status.

## Run

On Windows, `start.bat` or `./start.ps1` creates the local virtual environment
and starts the HMI. Manual setup:

```powershell
python -m pip install -r requirements.txt
python main.py
```

## Indicator selection persistence

All eight Screen B families keep indicator checklist state in browser local
storage. Timeframe changes do not rebuild `page-content` for Prices, CVD and
Open Interest; their dedicated callbacks update the figures instead. An
explicit empty selection is valid state and is never replaced by defaults. The
same selection therefore survives timeframe changes, contract rereads, A/B
navigation and browser refreshes until the user changes it.

## Final UI corrections

- Prices generic OHLCV volume is colored by candle direction when no true taker buy/sell split exists.
- CVD and ETF Screen B include native indicator selectors in the right column.
- On-Chain and Volatility right summaries are current-state snapshots; 7D/30D changes historical charts only.
- Liquidations Screen B consumes the 1D/7D/30D range selector and reports when the requested window exceeds the frozen 500×15m history available in the contract.
- While a manual family is updating, contract-completion polling runs every 250 ms.
- Contextual Help uses one persistent overlay outside refreshed card trees, so
  an open description does not blink or remount during contract refresh.


## Grok Radar

`/grok-radar` is a contractless, on-demand route. It uses `XAI_API_KEY` from the process environment and does not participate in the eight market-family contract refresh pipeline.

## C9 — Stacks/sBTC (Pre-Implementation Specification)

**Status: Planned / In Development.** The current public HMI remains C1-C8.
No Stacks adapter, operational Stacks data pipeline, versioned C9 contract, or
Stacks C9 HMI exists in this repository today. Those components are proposed
grant deliverables whose implementation begins in Milestone 1; this repository
update contains documentation only and does not claim grant approval.

The proposed C9 pipeline is:

`Stacks/sBTC -> Adapter -> Normalization -> Versioned C9 Contracts -> Financial Observables -> HMI`

C9 defines three committed observables: sBTC Supply & Peg State, sBTC Bridge
Flow, and sBTC Bridge Operational State. A separate proposed transversal source
surface describes how future C9 observables may provide context to C1-C8 without
changing ownership or metric semantics.

The shared pre-implementation C9 catalog contains 14 proposed logical source surfaces: 6 Core and 8 transversal. None is implemented or rendered by the current Screen HMI.

**C9 owns the Stacks/sBTC data, while C1–C8 can consume C9 observables for cross-family contextual analysis.**

- [C9 Stacks Technical Specification](docs/c9/stacks/C9_STACKS_TECHNICAL_SPEC.md)
- [C9 Stacks Endpoint Catalog](docs/c9/stacks/C9_STACKS_ENDPOINT_CATALOG.md)

## Equities Multi-Market Extension (Pre-Implementation)

**Status: PRE-IMPLEMENTATION / PROPOSED / NOT IMPLEMENTED.** The proposed
Equities extension preserves one HMI and introduces the conceptual navigation
`Market -> Asset -> C-family`. SSL Market/IBKR is treated only as a candidate
source of market observables; it is not a separate HMI.

The existing BTC/CRYPTO inventory remains frozen at **33 logical endpoints
across C1-C8**. Proposed Equities observables are additive, are not counted in
that inventory, and are not part of C9. No Equities selector, route, JSON
contract, fixture, provider connection, or HMI view is implemented by this
documentation update. Future Screen work must begin from a versioned Processing
contract aligned with Emulator and Integration.

Screen now contains a non-operational multi-market navigation scaffold and a
strict compatibility validator for Processing's 37 proposed `eq_c*`
observables and `vr1-observation-v1` projection. This does not activate an
Equities provider or family contract; Equities selections remain visibly
`NOT IMPLEMENTED` until the shared runtime path is validated.

- [Equities Screen Technical Specification](docs/equities/EQUITIES_SCREEN_TECHNICAL_SPEC.md)
- [Equities Observable Catalog](docs/equities/EQUITIES_OBSERVABLE_CATALOG.md)
- [Equities HMI Migration Map](docs/equities/EQUITIES_HMI_MIGRATION_MAP.md)
